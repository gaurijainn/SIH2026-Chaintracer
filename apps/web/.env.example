# Leave empty to use the same-origin proxy (/api -> API_PROXY, default http://localhost:4000)
VITE_API_BASE_URL=
# Optional: force the data-mode indicator (live | record | replay). Otherwise read from /health.
VITE_DATA_MODE=
